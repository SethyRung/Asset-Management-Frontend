export enum ResponseStatusCode {
  OK = "0",
  UNAUTHORIZED = "1000",
  BAD_REQUEST = "1001",
  FORBIDDEN = "1002",
  INTERNAL_SERVER_ERROR = "1003",
}

export enum Role {
  ADMIN = "ADMIN",
  USER = "USER",
  AUDIT = "AUDIT",
}
